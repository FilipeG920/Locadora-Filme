<%= form_with(model: [:admin, genero], local: true, class: "card p-4 shadow-sm") do |f| %>
  <div class="mb-3">
    <%= f.label :nome, "Nome do Gênero", class: "form-label" %>
    <%= f.text_field :nome, class: "form-control", required: true %>
  </div>

  <%= f.submit "💾 Salvar", class: "btn btn-primary" %>
<% end %>
