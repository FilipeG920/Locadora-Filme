<div class="container mt-5">
  <h2 class="text-center mb-4">🎞️ <%= @genero.nome %></h2>

  <% if @filmes.any? %>
    <div class="row">
      <% @filmes.each do |filme| %>
        <div class="col-md-4 mb-4">
          <div class="card h-100 shadow-sm">
            <div class="card-body d-flex flex-column">
              <h5 class="card-title"><%= filme.titulo %></h5>
              <p class="text-muted small mb-2"><%= filme.ano_lancamento %> — <%= filme.duracao %> min</p>
              <p class="card-text flex-grow-1"><%= truncate(filme.sinopse, length: 120) %></p>
              <%= link_to "Ver detalhes", filme_path(filme), class: "btn btn-outline-primary btn-sm mt-auto" %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <p class="text-center text-muted">Nenhum filme encontrado neste gênero.</p>
  <% end %>

  <div class="text-center mt-4">
    <%= link_to "⬅️ Voltar aos Gêneros", generos_path, class: "btn btn-secondary" %>
  </div>
</div>

<p style="color: green"><%= notice %></p>

<%= render @genero %>

<div>
  <%= link_to "Edit this genero", edit_genero_path(@genero) %> |
  <%= link_to "Back to generos", generos_path %>

  <%= button_to "Destroy this genero", @genero, method: :delete %>
</div>
