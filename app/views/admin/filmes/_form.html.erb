<%= form_with(model: [:admin, filme], local: true, class: "card p-4 shadow-sm") do |f| %>
  <div class="mb-3">
    <%= f.label :titulo, "Título", class: "form-label" %>
    <%= f.text_field :titulo, class: "form-control", required: true %>
  </div>

  <div class="mb-3">
    <%= f.label :sinopse, "Sinopse", class: "form-label" %>
    <%= f.text_area :sinopse, rows: 4, class: "form-control" %>
  </div>

  <div class="mb-3">
    <%= f.label :ano_lancamento, "Ano de Lançamento", class: "form-label" %>
    <%= f.number_field :ano_lancamento, class: "form-control", min: 1900, max: 2100 %>
  </div>

  <div class="mb-3">
    <%= f.label :duracao, "Duração (minutos)", class: "form-label" %>
    <%= f.number_field :duracao, class: "form-control", min: 1 %>
  </div>

  <div class="mb-3">
    <%= f.label :genero_id, "Gênero", class: "form-label" %>
    <%= f.collection_select :genero_id, Genero.all, :id, :nome, prompt: "Selecione um gênero", class: "form-select" %>
  </div>

  <%= f.submit "💾 Salvar", class: "btn btn-primary" %>
<% end %>
