<h1 class="mb-4 text-primary">🎭 Gerenciar Gêneros</h1>

<div class="mb-3 d-flex flex-wrap gap-2">
  <%= link_to "➕ Novo Gênero", new_admin_genero_path, class: "btn btn-success" %>
  <%= link_to "⬇️ Exportar CSV", admin_generos_path(format: :csv), class: "btn btn-outline-secondary" %>
</div>

<div class="card mb-4">
  <div class="card-body">
    <h2 class="h5 card-title">Importar gêneros via CSV</h2>
    <p class="text-muted mb-3">O arquivo deve conter a coluna <code>nome</code>; colunas adicionais serão ignoradas.</p>
    <%= form_with url: import_admin_generos_path, method: :post, local: true, html: { multipart: true, class: "row g-3 align-items-center" } do |form| %>
      <div class="col-md-6">
        <%= form.file_field :file, accept: ".csv", class: "form-control" %>
      </div>
      <div class="col-md-auto">
        <%= form.submit "⬆️ Importar CSV", class: "btn btn-outline-success" %>
      </div>
    <% end %>
  </div>
</div>

<table class="table table-bordered table-hover align-middle">
  <thead class="table-dark">
    <tr>
      <th>ID</th>
      <th>Nome</th>
      <th>Ações</th>
    </tr>
  </thead>
  <tbody>
    <% @generos.each do |genero| %>
      <tr>
        <td><%= genero.id %></td>
        <td><%= genero.nome %></td>
        <td>
          <%= link_to "✏️ Editar", edit_admin_genero_path(genero), class: "btn btn-sm btn-warning" %>
          <%= button_to "🗑️ Excluir", admin_genero_path(genero),
                        method: :delete,
                        data: { confirm: "Tem certeza que deseja excluir este gênero?" },
                        class: "btn btn-sm btn-danger" %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<div class="d-flex justify-content-between align-items-center flex-wrap gap-2 mt-3">
  <%= paginate @generos, theme: 'bootstrap-5' %>
  <%= link_to "⬅️ Voltar ao Dashboard", admin_root_path, class: "btn btn-secondary" %>
</div>
